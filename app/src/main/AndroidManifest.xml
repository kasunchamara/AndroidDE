<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.developer.exam">

    <!-- Android device permission
        @permission - Internet
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Launcher Activity -->
        <activity android:name="com.android.developer.exam.main.ActivityMain"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Hourly Count Activity -->
        <activity
            android:name="com.android.developer.exam.main.ActivityHourlyCount"
            android:configChanges="orientation|screenSize">
        </activity>

        <!-- Activity Monthly Sales Activity -->
        <activity
            android:name="com.android.developer.exam.main.ActivityMonthlySales"
            android:configChanges="orientation|screenSize">
        </activity>

    </application>
</manifest>